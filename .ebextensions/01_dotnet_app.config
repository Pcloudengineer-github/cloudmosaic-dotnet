# .ebextensions/01_dotnet_app.config
option_settings:
  aws:elasticbeanstalk:application:environment:
    # This environment variable tells the Kestrel server to listen on port 5000
    # for requests from any IP address, which is required for the Nginx proxy.
    ASPNETCORE_URLS: "http://*:5000"
